generator client {
  provider        = "prisma-client"
  output          = "../src/generated/prisma"
  moduleFormat    = "cjs"
}

datasource db {
  provider = "postgresql"
}

model Habitat {
  id        Int      @default(autoincrement()) @id
  title     String
  description   String?
  @@map("habitats")
}